# ERP SQL Server (1C database erp_pm). Read-only.
# Используйте hostname ii1 (192.168.1.157), не localhost и не сырой IP —
# Windows Auth (Trusted Connection) по IP даёт ошибку 18452.
ERP_SQL_SERVER=ii1
ERP_SQL_DATABASE=erp_pm
ERP_SQL_DRIVER=ODBC Driver 18 for SQL Server
ERP_SQL_ENCRYPT=no
ERP_SQL_TRUSTED_CONNECTION=yes
ERP_SQL_USER=
ERP_SQL_PASSWORD=

# JWT session for desktop clients
JWT_SECRET=change-me
JWT_ALGORITHM=HS256
JWT_EXPIRE_MINUTES=480

# API
API_HOST=0.0.0.0
API_PORT=7812

# LLM: stub | openai_compat (future)
LLM_PROVIDER=stub

# App Postgres (users / avatars). Separate from ERP SQL Server.
DATABASE_URL=postgresql+psycopg://constructor:constructor@127.0.0.1:5435/constructor
