# Локальный gateway (агенты, LLM, workflow) — Docker на этом ПК
BACKEND_URL=http://127.0.0.1:7812
# Общий сервер входа через 1С (ERP SQL). JWT_SECRET в infra/.env должен совпадать с этим сервером.
AUTH_URL=http://127.0.0.1:7812
