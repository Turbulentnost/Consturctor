[project]
name = "platform-tool-imap"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.115.0",
    "uvicorn[standard]>=0.32.0",
    "pydantic-settings>=2.6.0",
    "imapclient>=3.0.1",
    "celery>=5.4.0",
    "platform-contracts",
    "platform-db",
    "platform-service-common",
]

[project.scripts]
platform-tool-imap = "platform_tool_imap.main:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["platform_tool_imap"]

[tool.hatch.metadata]
allow-direct-references = true
