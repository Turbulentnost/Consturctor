[project]
name = "platform-tool-onec-com"
version = "0.1.0"
description = "32-bit COM microservice for 1C thin client (V83.Application)"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.115.0",
    "uvicorn[standard]>=0.32.0",
    "pydantic-settings>=2.6.0",
    "platform-contracts",
    "platform-service-common",
]

[project.optional-dependencies]
windows = ["pywin32>=306"]

[project.scripts]
platform-tool-onec-com = "platform_tool_onec_com.main:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["platform_tool_onec_com"]

[tool.hatch.metadata]
allow-direct-references = true
