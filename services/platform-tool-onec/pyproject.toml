[project]
name = "platform-tool-onec"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.115.0",
    "uvicorn[standard]>=0.32.0",
    "pydantic-settings>=2.6.0",
    "httpx>=0.28.0",
    "pyodbc>=5.2.0",
    "celery>=5.4.0",
    "platform-contracts",
    "platform-db",
    "platform-service-common",
]

[project.scripts]
platform-tool-onec = "platform_tool_onec.main:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["platform_tool_onec"]

[tool.hatch.metadata]
allow-direct-references = true
